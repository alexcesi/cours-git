name: CI Github Action
on:
  push:
    branches: [ master ]

jobs:
  build-react:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v2
      - name: Use Node.js 16.x
        uses: actions/setup-node@v1
        with:
          node-version: 16.x
      - name: Install dependencies
        run: npm install
      - name: Test Artefact 
        run: npm test
      - name: Build Artefact 
        run: npm run build
      - name: Deploy app
        run: netlify deploy --site ${SECRET_ID} --auth ${ACCESS_TOKEN} --dir build/ --prod